An AMD loader plugin for loading SVG resources
==============================================

Install:

```
bower install requirejs-svg
```

Use:

```
define(["requirejs-svg/svg!icon.svg", function() {
});
```
